[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "auto_service.settings"
python_files = ["test_*.py"]

[tool.isort]
profile = "black"
line_length = 100
known_first_party = ["auto_service"]

[tool.black]
line-length = 100
target-version = ["py310"]
include = '\.pyi?$'
exclude = '''
/(
    \.git

  | \.hg
  | \.mypy_cache
  | \.nox

  | \.tox
  | \.venv
  | venv

  | _build
  | buck-out
  | build

  | dist
  | __pypackages__
  | migrations
)/
'''